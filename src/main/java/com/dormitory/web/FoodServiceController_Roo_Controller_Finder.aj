// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.dormitory.web;

import com.dormitory.domain.FoodService;
import com.dormitory.domain.Room;
import com.dormitory.web.FoodServiceController;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;

privileged aspect FoodServiceController_Roo_Controller_Finder {
    
    @RequestMapping(params = { "find=ByRoomnumber", "form" }, method = RequestMethod.GET)
    public String FoodServiceController.findFoodServicesByRoomnumberForm(Model uiModel) {
        uiModel.addAttribute("rooms", Room.findAllRooms());
        return "foodservices/findFoodServicesByRoomnumber";
    }
    
    @RequestMapping(params = "find=ByRoomnumber", method = RequestMethod.GET)
    public String FoodServiceController.findFoodServicesByRoomnumber(@RequestParam("roomnumber") Room roomnumber, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, @RequestParam(value = "sortFieldName", required = false) String sortFieldName, @RequestParam(value = "sortOrder", required = false) String sortOrder, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("foodservices", FoodService.findFoodServicesByRoomnumber(roomnumber, sortFieldName, sortOrder).setFirstResult(firstResult).setMaxResults(sizeNo).getResultList());
            float nrOfPages = (float) FoodService.countFindFoodServicesByRoomnumber(roomnumber) / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("foodservices", FoodService.findFoodServicesByRoomnumber(roomnumber, sortFieldName, sortOrder).getResultList());
        }
        addDateTimeFormatPatterns(uiModel);
        return "foodservices/list";
    }
    
}
